%==============================================================================
% Abstract (150-250 words) + Index Terms
%==============================================================================

\begin{abstract}
% ---- 1) Context / motivation (1-2 sentences) -------------------------------
Recent advances in [your field, e.g., deep learning for medical imaging]
have enabled [capability], yet [unresolved problem] remains a major obstacle
to real-world deployment.
% ---- 2) Problem statement --------------------------------------------------
Existing approaches typically rely on [assumption / method], which
[limitation: e.g., requires large labeled datasets, fails under distribution
shift, is computationally expensive].
% ---- 3) Proposed method (1-2 sentences) ------------------------------------
In this paper, we propose \textbf{[METHOD NAME]}, a [type of approach]
that [key idea in plain language]. Specifically, we introduce
[component~1], [component~2], and [component~3], which together
[claimed benefit].
% ---- 4) Experiments & results ----------------------------------------------
Extensive experiments on [dataset~1] and [dataset~2] demonstrate that
our method achieves [headline metric, e.g., 92.4\% accuracy], outperforming
the strongest baseline by [\(+X.X\)\%] while reducing [cost metric] by
[\(Y\%\)].
% ---- 5) Contribution take-away ---------------------------------------------
Our results suggest that [insight], opening a path toward [broader impact].
Code and pretrained models will be released upon publication.
\end{abstract}

% ---- Index terms (4-6, alphabetically ordered) -----------------------------
\begin{IEEEkeywords}
Artificial intelligence, deep learning, [keyword~3],
[keyword~4], [keyword~5].
\end{IEEEkeywords}
