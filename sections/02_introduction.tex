%==============================================================================
% Section I: Introduction
%==============================================================================
\section{Introduction}
\label{sec:introduction}

% ---- Paragraph 1: Broad context & motivation -------------------------------
\IEEEPARstart{T}{he} rapid progress of [field, e.g., artificial intelligence]
has reshaped [application domain] over the past decade~\cite{lecun2015deep}.
In particular, [specific sub-area] has emerged as a critical enabler for
[real-world use case], with applications ranging from [example~A] to
[example~B].

% ---- Paragraph 2: The problem ----------------------------------------------
Despite this progress, [the central problem] remains poorly understood.
State-of-the-art methods such as~\cite{vaswani2017attention,he2016deep}
achieve impressive results on [benchmark], but suffer from
\textbf{(i)}~[limitation~1], \textbf{(ii)}~[limitation~2], and
\textbf{(iii)}~[limitation~3]. These limitations are particularly severe
in [setting where they matter], where [consequence].

% ---- Paragraph 3: Research gap & insight -----------------------------------
We argue that [the key insight that motivates the paper]. This observation
suggests that [the angle the paper takes], which has been largely overlooked
by prior work~\cite{devlin2019bert}.

% ---- Paragraph 4: Proposed approach ----------------------------------------
Building on this insight, we propose \textbf{[METHOD NAME]}
(\Cref{fig:overview}), a [type of method] that [one-sentence summary].
Our method consists of three main components:
\begin{itemize}
  \item \textbf{[Component~1]:} which [what it does and why].
  \item \textbf{[Component~2]:} which [what it does and why].
  \item \textbf{[Component~3]:} which [what it does and why].
\end{itemize}

% ---- Paragraph 5: Contributions --------------------------------------------
The main contributions of this paper are summarized as follows:
\begin{enumerate}
  \item We identify [the gap or new problem framing] and provide a
        principled analysis of why existing methods fall short.
  \item We propose \textbf{[METHOD NAME]}, a novel [approach type] that
        [the core technical idea].
  \item We conduct extensive experiments on [N] public benchmarks,
        showing that our method outperforms strong baselines by
        [\(+X\%\)] and reduces [cost] by [\(Y\%\)].
  \item We release our code, pretrained models, and reproducibility
        scripts to facilitate future research.
\end{enumerate}

% ---- Paragraph 6: Paper organization ---------------------------------------
The remainder of this paper is organized as follows.
\Cref{sec:related} reviews related work.
\Cref{sec:method} presents our proposed method in detail.
\Cref{sec:experiments} reports experimental results and ablation studies.
\Cref{sec:conclusion} concludes the paper and discusses future directions.

% ---- Placeholder figure ----------------------------------------------------
% \begin{figure}[t]
%   \centering
%   \includegraphics[width=\columnwidth]{overview.pdf}
%   \caption{High-level overview of the proposed method.}
%   \label{fig:overview}
% \end{figure}
