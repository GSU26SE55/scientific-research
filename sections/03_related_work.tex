%==============================================================================
% Section II: Related Work
%==============================================================================
\section{Related Work}
\label{sec:related}

We organize prior work into three threads most relevant to this paper:
(i)~[thread~A], (ii)~[thread~B], and (iii)~[thread~C].
We close the section by clarifying how our work differs from each thread.

% ---- Subsection A ----------------------------------------------------------
\subsection{[Thread A: e.g., Deep Learning for X]}
\label{subsec:related-a}

Early work on [Thread~A] focused on [classical approach]~\cite{lecun2015deep}.
Subsequent studies introduced [extension]~\cite{he2016deep}, achieving
substantial gains on [benchmark]. More recently, [trend, e.g.,
transformer-based architectures] have dominated this
line~\cite{vaswani2017attention,devlin2019bert}.
\emph{However}, these methods generally assume [assumption], which is
violated in [our setting].

% ---- Subsection B ----------------------------------------------------------
\subsection{[Thread B: e.g., Self-supervised Representation Learning]}
\label{subsec:related-b}

A complementary direction explores [Thread~B]. Representative methods
include [Method~1]~\cite{devlin2019bert} and [Method~2]~\cite{he2016deep},
which [what they do]. While effective, [shortcoming relative to our work].

% ---- Subsection C ----------------------------------------------------------
\subsection{[Thread C: e.g., Efficient Inference]}
\label{subsec:related-c}

Reducing the [cost / latency / memory] of [system] has been studied
extensively~\cite{vaswani2017attention}. Techniques range from
[technique~1] to [technique~2]. Unlike these works, we [our differentiator].

% ---- Positioning -----------------------------------------------------------
\subsection{Positioning of Our Work}
\label{subsec:related-positioning}

In contrast to the above lines of work, our method:
\begin{itemize}
  \item does not require [costly resource], unlike~\cite{he2016deep};
  \item generalizes to [setting], whereas~\cite{devlin2019bert} is limited
        to [restricted setting]; and
  \item offers a principled [theoretical/empirical] guarantee on
        [property], which is absent from prior art.
\end{itemize}
